Public Class frmMain

    Private Sub GroupBox1_Enter(sender As Object, e As EventArgs) Handles GroupBox1.Enter

    End Sub

    Private Sub frmMain_Load(sender As Object, e As EventArgs) Handles MyBase.Load

        ' ||Requirement||
        ' must be running with administrator priv.

        ' ||THINGS TO DESTROY||
        ' terminate gdrive process
        ' delete mydocument
        ' delete gdrive folder
        ' delete other than C:\ drive
        ' clear history, cache, temp, download folder
        ' delete important software
        ' --truecrypt
        ' --visual studio
        ' --vmware / virtualbox
        ' --quassel
        ' --vpn software
        ' --web server
        ' --torrent
        ' all deleting process must be wipe with 0 byte
        ' remove possible pirate software
        ' self-destroy appsz

    End Sub
End Class
